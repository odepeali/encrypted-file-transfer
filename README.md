what if your file could actually delete itself after being sent?
built a python secure file transfer tool that:
encrypts files with AES-256
Send them via TLS
automatically self deletes after a timer.

exploring how cryptography and networking combine to make privacy tools.
